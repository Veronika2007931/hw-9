// #1
 let time = 0;
  timerId = setInterval(() => {
     const alertEl = alert('hello')
     const times = time + 1
     console.log(times)
    }, 1000);
 
  if( timerId >= 5){
    clearInterval(timerId)
    }

// #5

const input = document.querySelector('.inputEl')


















